# Зависимости для тестирования
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-xdist>=3.3.0  # Параллельный запуск тестов
pytest-html>=4.0.0   # HTML отчёты
pytest-cov>=4.1.0    # Покрытие кода
pytest-timeout>=2.2.0  # Таймауты для тестов
httpx>=0.25.0        # HTTP клиент для тестов
websockets>=12.0     # WebSocket клиент для тестов чата
aiohttp>=3.9.0       # Async HTTP для тестов
faker>=20.0.0        # Генерация тестовых данных
pytest-order>=1.2.0  # Порядок выполнения тестов
